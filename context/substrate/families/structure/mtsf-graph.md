# MTSF Progressive Graph

**Match thin, mean thick:** T2 holds handles; `expand()` hydrates identity → configuration → evidence → substrate on demand.

Traversal `depth` is hop radius from start (depth=1 includes immediate neighbors).
