# MTSF Session Activation

Activation selects which shape configuration is live without changing entity identity. Snapshots on session close — not every token.

Pairs with `structure.mtsf.kernel` for condition evaluation.
