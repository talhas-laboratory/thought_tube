# MTSF Session Ingest

Fast mode uses heuristic phrase detectors. Deep mode resolves through the extraction skill stack (agent extractor by default, OpenClaw when available).

Ingest is the **pipeline entry** after T0 substrate is captured as session events.
