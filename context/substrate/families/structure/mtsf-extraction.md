# MTSF Extraction Validation

Owns the **validation sandwich** for semantic proposals: schema, evidence spans, stencil grammar, confidence gates.

Materialize always attempts graph build even when projection is blocked by validation errors.
