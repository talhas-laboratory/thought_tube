Status: blocked
Owner: cursor-cloud-reviewer
# CAE-008 — Bounded ActiveState continuity

**Stage:** C
**Priority:** medium
**Depends on:** CAE-005A, CAE-007

## Outcome

Purpose, object, tension, posture, lens, branch, and scope persist only where the effective envelope permits.

## Acceptance

- versioned state transition contract;
- multi-turn and cross-adapter continuity tests;
- state contains references, not copied ocean content;
- incognito leaves no durable state;
- rollback/compensating operation recorded.

## Verification

- **Commit:** `6c34f1b` on branch `cursor/cognitive-aperture-gap-map-24c7` (PR #32).
- **Command:** `. .venv/bin/activate && PYTHONPATH=src pytest -q tests/test_active_state_continuity.py`
- **Result:** 6 tests pass for transition contract, multi-turn carry, incognito ephemeral behavior, and Holodeck workspace read path.
- **Changed paths:** `src/conversation_os/active_state_continuity.py`, Bridge bundle wiring, Holodeck `holodeck_load_active_state_continuity()`, `tests/test_active_state_continuity.py`.
- **Feature flag:** `disclosure.active_state.continuity_v1` (default `false`).
- **Decision:** D-020.
- **Blocker:** D-023 — `rollback_active_state_transition()` re-asserts the compensated snapshot instead of restoring `prior_snapshot_id`; multi-turn undo is a no-op. Live blocker `blocker-5fde7e6b3515`. Stage C/D review (`5ac2934`) left task **blocked**.

## Rollback / risk

- Set `disclosure.active_state.continuity_v1: false`; call `rollback_active_state_transition()` only after rollback semantics are fixed.
- Residual risk: do not enable in production until T1→T2→rollback(T2) regression proves predecessor restoration.
