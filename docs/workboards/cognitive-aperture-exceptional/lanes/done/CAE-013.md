Status: done
Owner: cursor-cloud-cae013
# CAE-013 — Corpus readiness contract and fixture corpus

**Stage:** A
**Priority:** critical
**Depends on:** CAE-000
**Owner paths:** library/runtime status owner, fixture data, readiness tests

## Outcome

The aperture can distinguish ready, empty-valid, stale, interrupted, and unsupported corpus states before retrieval.

## Acceptance

- `CorpusCatalog` contract reports revision, counts, coverage, capabilities, and stale state;
- fixture corpus covers positive, negative, distractor, privacy, multi-dimensional Shape, and AntiMatch cases;
- raw source stored once with fragments and provenance;
- readiness failure produces abstention;
- no production corpus content committed as a test fixture.

## Verification

- **Commit:** `64f7f41` on branch `cursor/cognitive-aperture-gap-map-24c7` (PR #32).
- **Command:** `. .venv/bin/activate && PYTHONPATH=src pytest -q tests/test_conversation_os.py -k corpus_catalog`
- **Result:** readiness states `ready`, `empty_valid`, `stale`, `interrupted`, `unsupported` distinguished; abstention before retrieval when not ready; synthetic fixtures only.
- **Changed paths:** `library_tracker.py` (`build_corpus_catalog`), `runtime_pipeline.py`, corpus catalog tests in `tests/test_conversation_os.py`.
- **Fixture corpus:** `cognitive_aperture_chat_converter_v1` (synthetic chat-converter fixtures, not production dumps).
- **Decision:** recorded in GAP_MAP / CHAT_CONVERTER_SEED_CORPUS_V1 (no separate D-0xx).
- **Reviewer:** Stage A review approval (`900c6d5`).

## Rollback / risk

- Readiness contract is additive; disabling enforcement flags in downstream tasks avoids abstention without removing catalog.
- Residual risk: stale detection depends on pipeline refresh cadence and provenance completeness.
