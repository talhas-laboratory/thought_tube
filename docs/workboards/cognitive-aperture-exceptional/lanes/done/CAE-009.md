Status: done
Owner: cursor-cloud-reviewer
# CAE-009 — Feed adapter

**Stage:** D
**Priority:** medium
**Depends on:** Stage C gate

## Outcome

Feed evidence selection uses the shared disclosure contract while feed presentation remains surface-owned.

## Acceptance

- no parallel admission policy;
- per-post grant and provenance preserved;
- feed behavior, latency, feature flag, and rollback tested;
- surface owner approves projection.

## Verification

- **Commit:** `ca2bff5` on branch `cursor/cognitive-aperture-gap-map-24c7` (PR #32).
- **Command:** `. .venv/bin/activate && PYTHONPATH=src pytest -q tests/test_feed_disclosure_parity.py`
- **Result:** 8 tests pass — Bridge retrieval subset parity, `_select_contextual_candidate_pairs()` routes through disclosure when enabled, legacy meta-edge path preserved when disabled, per-post `disclosure_grant` / `disclosure_provenance` on promotion rows.
- **Changed paths:** `src/conversation_os/feed_disclosure_adapter.py`, `product_inner_world.py` (evidence selection branch only), `tests/test_feed_disclosure_parity.py`, `product/inner_world_v1/config/runtime.json`.
- **Feature flag:** `feed.disclosure_service_v1` (default `false`).
- **Decision:** D-022.
- **Reviewer:** Stage C/D independent review (`5ac2934`) — approved.

## Rollback / risk

- Set `feed.disclosure_service_v1: false` to restore `select_candidate_pairs()` leg; bubble pairing and `build_thought_feed()` presentation unchanged.
- Residual risk: feed pairs are synthesized from admitted capsules; full feed E2E on production corpus not yet baseline-certified.
