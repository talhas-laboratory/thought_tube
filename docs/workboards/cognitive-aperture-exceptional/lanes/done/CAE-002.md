Status: done
Owner: cursor-stage-b-review
# CAE-002 — Execution/audit isolation

**Stage:** B
**Priority:** critical
**Depends on:** CAE-015, CAE-006A
**Owner paths:** `chat_backends.py`, disclosure contracts, inspect path, focused tests

## Outcome

Model-bound objects cannot represent suppressed or omitted content; audit retains reasons through references.

## Acceptance

- `ExecutionBundle` contains no suppressed/omitted fields;
- unique suppression sentinels are absent from every model-bound payload;
- inspect/audit reconstructs omission reasons;
- sensitive suppressed text is not duplicated in receipts;
- feature flag and rollback verified.

## Residual risk

Alternate backend composers may bypass the primary path; enumerate and test every execution adapter.

## Verification

- **Commit:** `38c7806` on branch `cursor/cognitive-aperture-gap-map-24c7` (PR #32).
- **Command:** `. .venv/bin/activate && PYTHONPATH=src pytest -q tests/test_execution_audit_isolation.py tests/test_reasoning_runtime_agent_bridge.py`
- **Result:** execution/audit split verified — `frame_bundle` excludes suppressed fields; `frame_audit` owns omissions; suppression sentinels absent from model-bound payloads.
- **Changed paths:** `reasoning_bridge.py` (`split_frame_assembly`, `build_frame_audit`), `chat_backends.py`, `tests/test_execution_audit_isolation.py`.
- **Feature flag:** `bridge.execution_audit_isolation_v1` (default `true`).
- **Decision:** D-012.

## Rollback / risk

- Set `bridge.execution_audit_isolation_v1: false` to restore legacy combined frame assembly.
- Residual risk: enumerate every backend composer path; non-Bridge surfaces may still need audit isolation adapters.
