Status: done
Owner: cursor-stage-b-review
# CAE-001 — Fail-empty admission

**Stage:** B
**Priority:** critical
**Depends on:** CAE-013, CAE-014, CAE-006A, CAE-003A
**Owner paths:** `knowledge_layer.py`, candidate adapter, focused tests

## Outcome

Only candidates with explicit positive relevance evidence may enter ranking; no-match returns an explicit empty result.

## Plan

1. Separate admission signals from ranking features.
2. Record shadow decisions against the baseline corpus.
3. Backfill missing pond/address metadata identified by shadow results.
4. Enforce behind a rollback flag.

## Acceptance

- unrelated and empty bounded/strict queries return empty;
- confidence-only candidates are rejected;
- aliases, pins, and gold Shape/evidence fixtures preserve approved recall;
- stale/unready indexes abstain;
- positive/negative metrics and rollback evidence recorded.

## Residual risk

Recall regression; controlled by shadow mode, metadata backfill, and positive-suite gate.

## Verification

- **Commit:** `cd9f21c` on branch `cursor/cognitive-aperture-gap-map-24c7` (PR #32).
- **Command:** `. .venv/bin/activate && PYTHONPATH=src pytest -q tests/test_fail_empty_admission.py`
- **Result:** fail-empty admission tests pass — unrelated/empty bounded queries return `empty_no_positive_match`; confidence-only candidates rejected; shadow decisions recorded in `shadow_admission`.
- **Changed paths:** `src/conversation_os/candidate_admission.py`, `knowledge_layer.py`, `tests/test_fail_empty_admission.py`, `product/inner_world_v1/config/runtime.json`.
- **Feature flags:** `knowledge.fail_empty_admission_shadow_v1`, `knowledge.fail_empty_admission_enforce_v1` (both default `true` in shipped runtime).
- **Decision:** D-014.
- **Reviewer:** Stage B independent review — approved with Stage B gate (`8a2513e` parent closure).

## Rollback / risk

- Set `knowledge.fail_empty_admission_enforce_v1: false` to restore legacy ranking-without-admission path; shadow can remain on for observation.
- Residual risk: recall regression on near-neighbour queries; monitor via CAE-006A baseline known_failure.
