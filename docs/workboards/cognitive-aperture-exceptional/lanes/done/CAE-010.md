Status: done
Owner: cursor-cloud-reviewer
# CAE-010 — Optional task-pack evidence adapter

**Stage:** D
**Priority:** medium
**Depends on:** Stage C gate

## Outcome

Task packs may receive bounded evidence enrichment without replacing curated narrative handoff or atlas readiness gates.

## Acceptance

- unrelated requests do not receive fallback filler;
- narrative what/why/decided/open structure remains primary;
- evidence section is optional, bounded, and provenance-backed;
- feature flag and rollback tested.

## Verification

- **Commit:** follow-up commit on branch `cursor/cognitive-aperture-gap-map-24c7` (PR #32).
- **Command:** `. .venv/bin/activate && PYTHONPATH=src pytest -q tests/test_task_pack_disclosure_parity.py`
- **Result:** 6 tests pass — Bridge retrieval subset parity, unrelated queries omit `bounded_evidence`, flag-off preserves legacy pack shape, empty matches omit optional section.
- **Changed paths:** `src/conversation_os/task_pack_disclosure_adapter.py`, `src/conversation_os/routing.py` (`build_task_pack()` enrichment + optional markdown section), `tests/test_task_pack_disclosure_parity.py`, `product/inner_world_v1/config/runtime.json`.
- **Feature flag:** `task_pack.disclosure_service_v1` (default `false`).
- **Decision:** D-026.

## Rollback / risk

- Set `task_pack.disclosure_service_v1: false` to restore pack JSON/markdown without `bounded_evidence`; curated sessions/cards/concepts sections unchanged.
- Residual risk: evidence blocks are query-token matched only; unrelated corpus capsules never fill the optional section.
