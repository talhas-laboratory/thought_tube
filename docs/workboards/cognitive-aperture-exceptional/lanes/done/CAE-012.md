Status: done
Owner: cursor-cloud
# CAE-012 — Cross-surface metrics/operator view

**Stage:** D
**Priority:** low
**Depends on:** CAE-006B

## Outcome

Operators can compare aperture quality, empty/abstention reasons, leakage, budgets, and latency without exposing sensitive evidence.

## Acceptance

- metrics derive from receipts/baselines rather than a new truth store;
- privacy-preserving aggregation;
- Bridge/Holodeck and later adapters comparable by revision;
- dashboard has no control-plane mutation path unless separately authorized.

## Verification

- **Commit:** `421a664` on branch `cursor/cognitive-aperture-gap-map-24c7` (PR #32).
- **Command:** `. .venv/bin/activate && PYTHONPATH=src pytest -q tests/test_aperture_operator_metrics.py`
- **Result:** 8 tests pass — privacy-preserving receipt aggregation, baseline snapshot load, cross-surface revision comparison, read-only inspect when flag enabled, no control-plane mutation imports.
- **Changed paths:** `src/conversation_os/aperture_operator_metrics.py`, `reasoning_bridge.py` (`inspect_aperture_operator_view`), `holodeck.py` (`holodeck_inspect_aperture_operator_view`), `miniapp.py` (GET `/aperture/operator-view`), `tests/test_aperture_operator_metrics.py`, `product/inner_world_v1/config/runtime.json`.
- **Feature flag:** `disclosure.operator_metrics.operator_metrics_v1` (default `false`); requires `disclosure.receipts.persistent_receipts_v1` for live receipt aggregation.
- **Baseline inputs:** `derived/baselines/chat_converter_seed_v1.json`, `chat_converter_seed_v1_service.json` (corpus revision `db340a77323741710f5f2a9512123271505c13880a3f72ac4c3e11c19fc4ccad`).
- **Decision:** D-024.

## Rollback / risk

- Set `disclosure.operator_metrics.operator_metrics_v1: false`; inspect endpoints return `{enabled: false, read_only: true}`.
- Residual risk: operator view is only as complete as persisted receipts; surfaces without receipt recording show zero counts until their adapters record receipts.
