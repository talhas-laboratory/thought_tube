Status: done
Owner: cursor-cloud-cae015
# CAE-015 — Version disclosure contracts

**Stage:** A
**Priority:** critical
**Depends on:** CAE-000
**Owner paths:** contract owner selected by guard, contract fixtures

## Outcome

Version `ApertureRequest`, `ActiveStateSnapshot`, `RequestedGrant`, `EffectiveGrant`, `CandidateRef`, `EvidenceBlock`, `ExecutionBundle`, `AuditReceipt`, and result statuses.

## Acceptance

- field types, cardinality, nullability, and provenance defined;
- deny precedence and envelope defaults defined;
- execution contract cannot represent suppression;
- receipt retention and incognito behavior defined;
- forward/backward fixture compatibility tested;
- no storage/provider details leak into public contracts.

## Verification

- **Commit:** `3d09853` on branch `cursor/cognitive-aperture-gap-map-24c7` (PR #32).
- **Command:** `. .venv/bin/activate && PYTHONPATH=src pytest -q tests/test_disclosure_contracts.py`
- **Result:** all public contract types and result statuses validated; `ExecutionBundle` rejects suppression fields; incognito receipt retention `hashes_metrics_only`; fixture compatibility covered under `tests/fixtures/disclosure_contracts/v1/`.
- **Changed paths:** `disclosure_contracts.py`, contract fixtures, `tests/test_disclosure_contracts.py`.
- **Contract version:** `1.0`.
- **Decision:** D-010.
- **Reviewer:** Stage A gate approval (`06bf815`).

## Rollback / risk

- Contracts are type-boundary enforcement; downstream modules must not bypass `normalize_effective_grant()` or receipt validation.
- Residual risk: forward compatibility depends on fixture updates when contract version bumps.
