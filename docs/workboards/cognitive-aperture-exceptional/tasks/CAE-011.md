Status: done
Owner: cursor-cloud-reviewer
# CAE-011 — Kernel bounded-view wire-or-demote

**Stage:** D
**Priority:** medium
**Depends on:** Stage C gate

## Outcome

Record and execute an honest decision: use bounded view as a granted epistemic backend, or document it as separate and remove integration claims.

## Acceptance

- explicit ADR;
- if wired, one branch/scope/provenance conformance test and rollback;
- if demoted, product and architecture claims updated;
- no duplicated canonical records.

## Verification

- **Commit:** `0a2e9b0` on branch `cursor/cognitive-aperture-gap-map-24c7` (PR #32).
- **Command:** `. .venv/bin/activate && PYTHONPATH=src pytest -q tests/test_bounded_view_disclosure_adapter.py`
- **Result:** 6 tests pass — branch/scope isolation between competing kernel branches, reference-only evidence blocks with no foundation store mutation, missing branch/scope abstention, flag-off rollback, port routing via `BoundedViewEpistemicPort`.
- **Changed paths:** `src/conversation_os/bounded_view_disclosure_adapter.py`, `src/conversation_os/disclosure_ports.py`, `tests/test_bounded_view_disclosure_adapter.py`, `product/inner_world_v1/config/runtime.json`, `docs/workspaces/cognitive-aperture-exceptional/derived/ADR-003-kernel-bounded-view-epistemic-backend.md`.
- **Decision:** D-027 (wire as optional epistemic backend; ADR-003).
- **ADR:** ADR-003 — kernel bounded view wired as default-off optional backend, not demoted.

## Rollback / risk

- Set `disclosure.bounded_view.epistemic_backend_v1: false` (default) to disable all bounded-view evidence collection.
- Residual risk: adapter is port-exposed only; Bridge/Holodeck default bundles do not yet consume epistemic blocks until a surface owner opts in.
