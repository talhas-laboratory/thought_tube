Status: done
Owner: cursor-stage-b-review
# CAE-004 — Orient-first compose

**Stage:** B
**Priority:** high
**Depends on:** CAE-002, CAE-003A, CAE-003B
**Owner paths:** bridge state/orientation owner, `chat_backends.py`, snapshots

## Outcome

A bounded local-state orientation precedes constraints and evidence without using undisclosed global material.

## Acceptance

- compose order is orientation → constraints → evidence → user turn;
- orientation has a hard cap;
- no-evidence response remains coherent;
- any second-pass widen requires a new grant and receipt;
- snapshots and privacy tests pass.

## Verification

- **Commit:** `54a74a8` on branch `cursor/cognitive-aperture-gap-map-24c7` (PR #32).
- **Command:** `. .venv/bin/activate && PYTHONPATH=src pytest -q tests/test_orient_first_compose.py`
- **Result:** compose order orientation → constraints → evidence → user turn; capped orientation; empty-evidence guidance; second-pass widen requires grant id in caller hints.
- **Changed paths:** `orient_first_compose.py`, `reasoning_bridge.py`, `chat_backends.py`, `tests/test_orient_first_compose.py`.
- **Feature flag:** `bridge.orient_first_compose_v1` (default `true`).
- **Decision:** D-016.

## Rollback / risk

- Set `bridge.orient_first_compose_v1: false` to restore legacy compose ordering.
- Residual risk: ActiveState continuity (CAE-008) is default-off; enable only after operator review of rollback semantics (D-025).
