Status: done
Owner: cursor-cloud-reviewer
# CAE-007 — Receipts and observability

**Stage:** C
**Priority:** high
**Depends on:** CAE-005A
**Owner paths:** receipt owner, Bridge/Holodeck inspect paths

## Outcome

Every disclosed, empty, denied, or abstained result is reconstructible from a compact privacy-aware receipt.

## Acceptance

- receipt records corpus/policy revisions, effective grant, decisions, block IDs, budget ledger, and result status;
- sensitive evidence text is referenced rather than duplicated;
- incognito stores hashes/metrics only;
- retention and deletion behavior tested;
- one full result reconstruction demonstrated.

## Verification

- **Commit:** `ed7d5e3` on branch `cursor/cognitive-aperture-gap-map-24c7` (PR #32).
- **Command:** `. .venv/bin/activate && PYTHONPATH=src pytest -q tests/test_disclosure_receipts.py`
- **Result:** 7 tests pass — persistence, incognito hashes-only retention, Bridge/Holodeck inspect, and `reconstruct_disclosure_result()` demonstrated.
- **Changed paths:** `src/conversation_os/disclosure_receipts.py`, `disclosure_ports.py` (`_DefaultReceiptSink`), Bridge/Holodeck inspect wrappers, `tests/test_disclosure_receipts.py`.
- **Feature flag:** `disclosure.receipts.persistent_receipts_v1` (default `false`).
- **Storage:** `product/inner_world_v1/data/reasoning_runtime/disclosure_receipts.jsonl`.
- **Decision:** D-019.
- **Reviewer:** Stage C/D independent review (`5ac2934`) — approved.

## Rollback / risk

- Set `disclosure.receipts.persistent_receipts_v1: false`; existing JSONL is retained but new receipts are not appended.
- Residual risk: receipt materialization runs on Bridge path even when persistence is off; operators must enable flag for cross-surface aggregation (CAE-012).
