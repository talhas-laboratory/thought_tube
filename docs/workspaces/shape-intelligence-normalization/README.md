# Shape Intelligence — Normalization

Child of [Shape Intelligence Population](../shape-intelligence-population/README.md). Owns the deterministic `normalize_source` capability: lossless parsing, stable source/segment IDs, offsets, structure, metadata, and deterministic redaction/classification. It never interprets meaning or identifies Shapes.
